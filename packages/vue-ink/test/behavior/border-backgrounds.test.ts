import { describe, it } from 'vitest';

// TODO: port from repos/ink/test/border-backgrounds.tsx — border edges with
// independent background colors per side, fallback to a shared
// borderBackgroundColor, no-bleed into content rows.

describe('border-backgrounds', () => {
	it.todo('border with background color');
	it.todo('border with different background colors per side');
	it.todo('border background color fallback to general borderBackgroundColor');
	it.todo('vertical border background does not bleed into content rows');
	it.todo('foreground, background and dim combine correctly');
});
