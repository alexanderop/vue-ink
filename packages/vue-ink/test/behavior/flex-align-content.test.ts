import { describe, it } from 'vitest';

// TODO: port from repos/ink/test/flex-align-content.tsx — alignContent
// default + stretch + omit/clear-on-rerender.

describe('flex-align-content', () => {
	it.todo('align content defaults to flex-start');
	it.todo('align content does not add extra spacing when there is no free cross-axis space');
	it.todo('clears alignContent on rerender to default flex-start');
	it.todo('clears alignContent from stretch on rerender to default flex-start');
	it.todo('clears alignContent when prop is omitted on rerender');
});
